# Tắt tự động audit ngầm khi install để tối ưu tốc độ CI và tránh lỗi rate limit từ registry npmjs
audit=false

# Tối ưu thời gian timeout và số lần retry kết nối mạng
fetch-retries=2
fetch-retry-mintimeout=3000
fetch-retry-maxtimeout=10000
